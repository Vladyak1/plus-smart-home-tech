spring:
  datasource:
    username: "main"
    password: "main"
    url: jdbc:postgresql://localhost:9438/postgres
    driver-class-name: "org.postgresql.Driver"

    sql:
      init:
        mode: always

server:
  port: 0